import React from "react";

const Offline = () => {
  return (
    <div
      style={{
        color: "red",
        fontSize: "2rem",
        textAlign: "center",
        marginTop: "2rem",
        width: "100%",
        height: "4rem",
        background: "rgba(255, 0, 0, 0.1)",
      }}
    >
      Offline
    </div>
  );
};

export default Offline;
